@model List<WebUI.Controllers.CategoryViewModel>
@{
    ViewData["Title"] = "Categories";
    var orderbyValue = Context.Request.Query["orderby"].ToString();
    var topValue = Context.Request.Query["top"].ToString();
    var skipValue = Context.Request.Query["skip"].ToString();
}

<h2 class="text-center mb-4">Categories</h2>
<div class="container">
    <form asp-action="Index" method="get" class="row g-2 mb-3">
        <div class="col-auto">
            <select name="orderby" class="form-select">
                <option value="">Sort by</option>
                @if(orderbyValue=="CategoryName") {
                    <option value="CategoryName" selected>Name</option>
                } else {
                    <option value="CategoryName">Name</option>
                }
                @if(orderbyValue=="CategoryId") {
                    <option value="CategoryId" selected>ID</option>
                } else {
                    <option value="CategoryId">ID</option>
                }
            </select>
        </div>
        <div class="col-auto">
            <input type="number" name="top" class="form-control" placeholder="Top" value="@topValue" min="1" />
        </div>
        <div class="col-auto">
            <input type="number" name="skip" class="form-control" placeholder="Skip" value="@skipValue" min="0" />
        </div>
        <div class="col-auto">
            <button type="submit" class="btn btn-primary">Apply</button>
        </div>
    </form>
    <table class="table table-striped table-bordered shadow-sm">
        <thead class="table-primary">
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Description</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.CategoryId</td>
                <td>@item.CategoryName</td>
                <td>@item.CategoryDesciption</td>
            </tr>
        }
        </tbody>
    </table>
</div>
